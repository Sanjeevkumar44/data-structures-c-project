# data-structures-c-project
A Data Structures project in C that demonstrates the implementation of a Circular Queue.  The program simulates a call center system where incoming calls are added to a queue and processed in FIFO order using enqueue and dequeue operations.
